doctype html
html
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            link(rel='stylesheet' href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            title Natours | #{title}

    body(data-alert=`${alert ? alert : ''}`)
        // HEADER
        include _header
        
        // CONTENT
        block content 
            h1 Heading that will be overwritten

        // FOOTER
        include _footer

        script(src='https://js.stripe.com/clover/stripe.js')
        script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.1.3/axios.min.js')
        script(src='/js/mapbox.js')
        script(type='module' src='/js/login.js')
        script(type='module' src='/js/signup.js')
        script(type='module' src='/js/updateSettings.js')
        script(type='module' src='/js/stripe.js')

//- NOTE: 
//- 1) Buffered code is that code displayed in the website like (h1, h2, p , etc...)
//- 2) Non-Buffered code is that code not displayed in the website and it starts with dash '-' (ex: - const x = 10)